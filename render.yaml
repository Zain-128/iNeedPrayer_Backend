# Render Blueprint — set this repo as the service root, or merge into your monorepo root.
services:
  - type: web
    name: ineedprayer-backend
    runtime: node
    buildCommand: npm install
    startCommand: npm start
